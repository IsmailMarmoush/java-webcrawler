package com.marmoush.analyst.analyst;

public class Report {
	public static String getFileReport(String filePath) {
		StringBuilder sb = new StringBuilder();
		// CrawlFile cf = new CrawlFile();
		// TreeSet<String> emails = cf.getEmails();
		// List<String> redundantDomains =
		// StringUtils.emailToDomainList(emails);
		// TreeSet<String> domains = StringUtils.emailToDomainsTree(emails);
		// sb.append("Total Number of EmailsGroup: " + emails.size());
		// sb.append("\n");
		// sb.append("Total Number of DomainsGroup: " + domains.size());
		// sb.append("\n");
		// List<List<String>> collection = StringUtils
		// .frequencyPair(redundantDomains);
		// sb.append(StringUtils.toString(collection, " : ", "\n"));
		return sb.toString();
	}
}
